# Docker Compose seed profile configuration
# Use with: docker compose --profile seed -f docker-compose.yml up
#
# This profile includes sample data in the PostgreSQL database for development.
# Start with: docker compose --env-file docker/.env.seed up
# Or: docker compose --profile seed up

COMPOSE_PROFILES=seed
